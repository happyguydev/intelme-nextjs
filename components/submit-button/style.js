import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  submitButton: {
    width: "437px",
    borderRadius: "10px",
    marginTop: "8px",
  },
  buttonLabel: {
    color: "white",
    fontFamily: "Poppins",
    fontSize: "14px",
    fontStyle: "normal",
    fontWeight: 600,
  },
});

export default useStyles;
